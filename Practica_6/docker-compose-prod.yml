version: '3.3'

services:
  webpage:
    container_name: webpage
    image: macochave/sa-practica:lts
    build:
      dockerfile: Dockerfile
      context: ./web-page/
    ports:
      - '8082:8080'
